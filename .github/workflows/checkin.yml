name: Scheduled Check-In Bot

permissions:
  contents: write

on:
  schedule:
    - cron: "0 10 * * *"   # runs daily at 10:00 UTC
    - cron: "0 18 * * *"   # runs again at 18:00 UTC
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    env:
      TOKEN: ${{ secrets.TOKEN }}
      BASE_URL: ${{ secrets.BASE_URL }}
      INSTRUCTOR_ID: ${{ vars.INSTRUCTOR_ID }}
      MY_USER_ID: ${{ vars.MY_USER_ID}}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
        run: python automated_bot.py

      - name: Commit artifact
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f artifact/
          git commit --allow-empty -m "Update artifact"
          git push
